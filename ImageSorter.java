import java.util.ArrayList;
import java.awt.Dimension;

public class ImageSorter {

	/* obviously this is not implemented yet.
	 * it may make more sense to have the planned functionality inside ImagePanel instead,
	 * but i would rather keep them somewhat modular.
	 * 
	 * the goal is to sort the pixels in the image that has been generated, and re-generate the image
	 */
	
	public ImageSorter() {
		
	}
	
	public ArrayList<Integer> sortImage(ArrayList<Integer> pixels, Dimension size) {
		ArrayList<Integer> sortedPixels = pixels;
		
		return sortedPixels;
	}
	
}
